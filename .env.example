# ── Auth ───────────────────────────────────────────────────────────────
# Long random string used to sign session JWTs (HS256). Cookie is host-only
# (NOT a .rayanemelzi.dev cookie — that would leak to all your other projects).
# Auto Quran shares accounts by connecting to the SAME database below.
AUTH_JWT_SECRET=generate-a-long-random-secret

# ── Database (Postgres) ────────────────────────────────────────────────
# Start it with: docker compose up -d
DATABASE_URL=postgres://chromaquran:chromaquran@localhost:5432/chromaquran

# ── Auto Quran backend URL ─────────────────────────────────────────────────
# Used for two things:
#   1. "Share on Instagram" (ExportModal) — uploads the rendered MP4 to
#      Auto Quran's POST /api/post-video, which publishes it to Instagram.
#   2. "Pipeline" tab — Next.js rewrites /api/pipeline/* to this URL so the
#      React tab can drive Auto Quran's full pipeline without CORS issues.
# Default is Auto Quran's local dev port. Change to the production URL on the VM.
# AUTOQURAN_API_URL=http://localhost:5000

# ── Rendering ──────────────────────────────────────────────────────────
# How many videos may render at once (keep low on a single box).
# CQ_RENDER_CONCURRENCY=1
